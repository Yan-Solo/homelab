apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tooljet
  namespace: apps
spec:
  rules:
  - host: tooljet.intropin.be
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: tooljet
            port:
              number: 3000

